{
  "version": 2,
  "buildCommand": "npx prisma generate && npx tsc",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
